# Backend dependencies
openai>=1.0.0
python-dotenv>=1.0.0
watchdog>=3.0.0

# NEW: FastAPI & REST API
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Optional: CORS support (already in FastAPI)
# python-jose>=3.3.0  # For JWT if needed in future
